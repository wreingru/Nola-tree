# Google Maps Platform API key with Street View Static API enabled.
# Never commit a real key. Copy this file to .env for local use.
GOOGLE_MAPS_API_KEY=

# Optional: use Google Cloud Vision for detection instead of the offline heuristic.
# TREE_DETECTOR=vision   # opencv (default) | vision
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account.json
